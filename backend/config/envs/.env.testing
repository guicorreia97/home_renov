# Committed on purpose: dummy values only, so tests and CI can boot the app.
# Never put a real credential in this file.
DATABASE_URL=json://./data/testing
SECRET_KEY=testing-not-a-real-secret
DEBUG=true
SPECIAL_MESSAGE=home_renov (testing)
