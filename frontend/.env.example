# Frontend configuration. Copy to .env.local (untracked) and adjust.
#
# Everything here ships to the browser: VITE_ variables are public by definition.
# Never put a credential, token, or connection string in this file or in
# .env.local. -> docs/secrets-guide.md
VITE_API_URL=http://localhost:8000
